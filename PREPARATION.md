# 제작 준비 보고 (2026-08-22)

> **§5 실측 결과(1차 녹화 분석)가 추가됨** — 0단계 항목 다수가 사용자 제공 녹화 1편에서 해결됐다.

스캐폴드 구축 + 설계·플랜 전수 검증 결과. 검증은 병렬 에이전트 3기(플랜 JSON 검증 / A군 해소 감사 / 미결항목 단계 매핑)로 수행.

## 1. 준비 완료 상태

- SPM 패키지 스캐폴드: `swift build`·`swift test` 통과. §3 구조 그대로 (`SCCoachKit` / 실행 파일 `SCCoach` / `SCCoachKitTests`). 세부는 [CLAUDE.md](CLAUDE.md).
- 툴체인: Xcode 26.6 / Swift 6.3 — 요구사항(Swift 5.9+, macOS 14+) 충족. **Go 미설치** — 9단계 screp 소스 빌드 전 필요.
- `regions-1920x1080.json` 자리표시자 생성 — rect 전부 0, 0단계 실측으로 기입.

## 2. 검증 결과

### 설계 문서 — A-1~A-7 전건 해소 확인 ✓

시뮬레이션 문서의 아키텍처 계약 공백 7건이 설계 v2.2에 모두 명문 규정으로 반영됨을 절별 인용으로 확인. **구현 착수에 설계 차단 요소 없음.**

경미한 문서 정비 대상 4건 발견 (설계 훼손 아님 — 문구·열거 수준):

| # | 위치 | 내용 |
|---|---|---|
| EXTRA-1 | §5.1 | 첫 문단 "위치 7"이 이전 버전 잔재 — 현재 존 라벨 계약은 12방위 + 내 기지 3종 |
| EXTRA-2 | 헤더 | 부록 A·시뮬레이션 문서는 "v2.2"를 참조하는데 헤더 변경 이력은 v2까지만 |
| EXTRA-3 | §4.2 | 주기 목록에 ResourceReader 0.5 누락 (§6.1·§6.5에는 있음) |
| EXTRA-4 | §8 | delay 기준점 "onDelivery 시점" 문구가 scout 스킵(effects 전진) 경로를 포괄 못함 — §4.3의 "lastStepDeliveredAtGame(발화 또는 scout 스킵)"으로 통일 표현 권장 |

### 플랜 JSON 11종 — 전부 스키마 통과 ✓

파싱·스키마·README 표 대응(저그 5·테란 3·프로토스 3)·supply 트리거·kind 규칙 모두 정상. error 0건, 지적 4건:

| 심각도 | 파일 | 내용 |
|---|---|---|
| warn | zerg-crazy | `"성큰 도배해"` — 규칙 4 어미 집합 이탈 (유일). "성큰 추가" 권장 |
| warn | zerg-crazy | `발업 눌러 delay 60` — 건물 완성이 아닌 자원 타이밍 용도 (규칙 3 모호) |
| info | zerg-9pool 등 | 같은 supply 순서 분리용 delay — §8 예시가 쓰는 패턴이라 사실상 승인. README 규칙 3 "에만" 문구만 §8과 정합 필요 |
| info | zerg-crazy | 챔버 1개로 공업+방업 — supply 34 방업 시점에 챔버가 공업 중일 가능성. "챔버 추가" 스텝 고려 |

## 3. 미결 항목 → 단계 매핑 (16건, blocking 8건)

| 단계 | blocking (완료 기준을 막음) | 비차단 (튜닝·확인) |
|---|---|---|
| 0단계 | C-1 flash 마스크 실측 · D-3 초록 RGB 구분 실측 | |
| 2단계 | | D-2 F10 메뉴 가림 확인 |
| 3단계 | **B-5** SupplyGate 첫 관측 채택 규칙 · **B-9** GameClock "2회 연속 일치"를 허용 오차로 재정의 | B-2 location=nil 공간화 · B-6 rate≈0 처리 |
| 4단계 | | D-1 래더 슬롯 로비 존재 여부 (없으면 래더는 항상 축소 모드 — 중대 함의) |
| 5단계 | **B-3** flash 전용 문장·종족별 문구 · **B-10** 존 반경 계약 | B-7 팔레트 토글 트랙 재시작 · C-2 urgent 체감 지연 · C-3 고정 팔레트 마스킹 픽스처 |
| 7단계 | **B-1** isInCombat 픽셀 임계 · **B-4** delay 기준점(설계에 이미 확정 — 반영만) | B-8 축소 모드 표기 정합 |

결정 시 **결정값을 설계 문서에 기입**한다 (CLAUDE.md 규약).

## 4. 0단계 — 사용자 실측 체크리스트 (실기 SC:R 필요)

§9의 7항목 + 시뮬레이션 D군 병합. 완료 기준: 결과가 설계 문서에 기입됨.

**채집은 동영상 녹화 권장** — 스크린샷 대신 게임 화면 녹화 mp4를 주면 픽스처 프레임 추출은 개발 측에서 수행한다. 영상 원본은 `ReplayFileSource` 회귀 테스트 입력으로도 그대로 쓰인다(§10). 깜빡임(3번)·팔레트 토글(6번)은 프레임 연속성이 필요해 영상 필수. 녹화 요령:

- macOS 화면 수록(⌘⇧5 또는 QuickTime)으로 **SC:R 창 영역**을 고화질 녹화 — 과도한 재압축은 인구수 숫자를 뭉갬
- 한 판 풀게임(로비 진입→게임→승/패 화면)이면 아래 1·4·9번과 supply·phase 픽스처가 한 번에 해결됨
- 게임 중 의도적으로 넣어줄 장면: 교전(미니맵 깜빡임), Shift+Tab 토글 왕복, F10 메뉴 열고 닫기
- 창 크기(포인트)를 함께 알려줄 것. 여유가 되면 다른 창 크기로 짧게 한 번 더(10번 검증용)

- [ ] 1. 시계 ON + 테두리 없는 창모드(평소 플레이하는 창 크기 — 크기를 기록할 것)에서 시계 위치·포맷(mm:ss) 스크린샷 → `Regions.clock`
- [ ] 2. Fastest에서 시계 vs 스톱워치 1~2분 병행 측정 → 환산 상수 (GameClock 폴백 전용)
- [ ] 3. 피격 경보 미니맵 깜빡임 화면 녹화(정탐) + 빨강 플레이어 병력 이동 녹화(오탐) → FlashDetector 마스크 확정, `Fixtures/flash/`
- [ ] 4. ScreenCaptureKit `.nominal` 1프레임 캡처로 버퍼 픽셀 == 창 포인트 검증 (§12.1)
- [ ] 5. 팀 매치메이킹 시작 직후 동맹 기지가 미니맵에 보이는지 스크린샷 (공유 시야 기본 여부)
- [ ] 6. Shift+Tab 양 팔레트 스크린샷 + 고정 팔레트 정확 RGB(동맹=노랑 확인) + 플레이어-초록 vs 고정-초록 구분 가능성 (D-3 병합)
- [ ] 7. 게임 종료 후 LastReplay.rep 실제 경로·쓰기 지연 확인 → ReplayWatcher
- [ ] 8. 1v1 래더 매칭에 슬롯 로비 화면이 있는지 확인 (D-1)
- [ ] 9. F10 메뉴가 supply 표시 영역을 가리는지 스크린샷 (D-2)
- [ ] 10. **같은 종횡비의 서로 다른 창 크기 2개**(예: 1920×1080과 1600×900)에서 같은 장면 스크린샷 → HUD(인구수·시계·미니맵)의 위치·크기가 비율대로 스케일되는지 확인 — §12.2 "종횡비 동일 → 비율 스케일" 경로의 숨은 전제 검증. 비율 스케일이 안 되면 창 크기별 regions JSON이 필요해짐

**창 크기 관련**: 창모드 크기는 수동 조절 가능하므로 기준 크기를 하나 정해 쓰는 것을 권장(어느 크기든 무방 — regions JSON의 referenceSize를 그 크기로 잡으면 됨). 스크린샷은 Retina에서 2배 픽셀로 저장될 수 있는데, 캡처 파이프라인은 1×(포인트 크기) 기준이므로 픽스처 제작 시 1×로 변환한다 — 채집자는 신경 쓸 필요 없음, 창 크기(포인트)만 함께 기록해 주면 됨.

**1단계(픽스처 인구수 인식)는 실측 없이 착수 가능** — 단, supply 픽스처 PNG 20장(막힘 직전·두/세 자리·종족 3종·교전 겹침)은 실기 스크린샷 채집이 필요하다. 4번 검증용 스크린샷과 겸용 채집을 권장.

---

## 5. 실측 결과 — 1차 녹화 분석 (2026-08-22)

원본: 15분 21초 풀게임 녹화(커스텀 UMS "Polypoid" 128×128, 컴퓨터 저그전, 플레이어 프로토스·**초록**, 1764×1280 @58fps). 게임 창 내용 영역 = (0,32)~1750×1242 (타이틀바 32px·우측 뒷창 14px 제외 — 픽셀 시차 분석으로 측정). 픽스처 127장을 `SCCoach/Tests/Fixtures/`에 적재.

| 체크리스트 | 결과 |
|---|---|
| 1. 시계 위치·포맷 ✅ | 하단 콘솔 우측 포드, `MM:SS` 흰색. 픽스처 좌표 (900,938,140,36) — `regions-1750x1242.json` |
| 2. 환산 상수 ✅ | **rate = 정확히 1.0** (t=100→01:27, t=850→13:57: Δ게임 750초/Δ스트림 750초). SC:R 시계는 Fastest에서도 실시간 1:1 — GameClock 폴백 상수·B-9 허용 오차 설계 단순화 |
| 3. 피격 경보 시그니처 ✅ | **"순수 빨강" 가설 기각.** 경보 = 소유 색 고휘도 토글: ON≈RGB(55,250,20)(초록 플레이어) ↔ OFF≈RGB(0,17,0), 주기 ~2.5Hz(ON 0.2s/OFF 0.2s). §6.2 마스크를 "고휘도·고채도 임계"로 재정의 필요. 정탐+대조 시퀀스 `Fixtures/flash/` 적재. 다중 위치 동시 깜빡임(시선 밖 2곳) 실측 — FlashDetector 전 클러스터 보고 설계의 실증 |
| 9. 대화상자 가림(D-2) ✅ | 임무 나가기 대화상자는 화면 중앙만 덮고 **supply는 계속 보임**(t=915, 64/165) → 잠정 ended 오발 경로 없음 |
| 4. `.nominal` 버퍼 검증 | 미해결 — 라이브 SCK 필요(2단계에서) |
| 5. 팀전 공유 시야 / 6. Shift+Tab 팔레트 / 7. LastReplay 경로 / 8. 래더 로비 / 10. HUD 비율 스케일 | 미해결 — 추가 녹화·확인 필요 |

**추가 발견 — 종료 흐름**: 메뉴 나가기로 종료 시 **중앙 승패 밴드가 없다**. 흐름: 대화상자(inGame 유지) → 전면 일러스트(시그니처 소실) → 점수 화면("패배!" + "소요 시간 15:12"). 즉 이 경로는 잠정 ended로만 잡힌다 — **점수 화면("패배!/승리!" 텍스트)을 ended(확정) 보조 시그니처로 추가 검토** (설계 §6.5 보강 후보). 점수 화면에서 내 이름은 "다크호스/예꾸", APM 94.

**창 크기 대응 정리** (사용자 질의 2건 반영 — 마우스 리사이즈라 크기 고정 불가): 좌표 해석은 3단으로 동작한다. ① 사용자 확인 캐시(`~/Library/Application Support/SCCoach/regions/`) 정확/스케일 매치 ② 번들 프로필 정확/스케일 매치 ③ **앵커 유도** — SC:R HUD가 "창 높이 비례 스케일 + 코너 앵커"(인구·자원=우상단, 미니맵=좌하단, 시계=하단 중앙 오프셋)라는 모델로 임의 크기의 좌표를 항상 만들어낸다. 유도 좌표는 미확인 상태로 쓰이다가 **창이 떠 있을 때 실신호(인게임 인구수 2연속 판독 = 인게임 전이)로 자동 확인**되면 캐시에 저장돼 다음 세션부터 매치로 승격된다. 검증: 합성 와이드 프레임(1950×1242 — 콘솔 중앙에 200px 삽입)에서 우상단·좌하단 앵커의 기능 검증 통과. **잔여 전제**: 코너 앵커·높이 비례 모델과 clock 중앙 오프셋·lobbySlots 배치는 다른 크기의 실게임 데이터가 없어 미검증(체크리스트 10번과 통합) — 실기에서 유도 좌표가 확인 실패하면(인게임인데 오렌지 상태 지속) 그 크기의 짧은 인게임 녹화 하나로 보정한다. 이번 녹화 내에서는 크기 변화 없음(경계 측정으로 확인).

**1단계 구현 노트 (2026-08-22 완료)** — Vision OCR 실측 특성 5가지, 구현 규칙의 근거:
1. `.fast`는 이 LED풍 폰트에서 관측 0건(3·6배 확대), 9배에서 슬래시를 쉼표로 오독 → §6.1의 `.fast` 지정 기각, `.accurate` 기본
2. CoreImage 임계 필터는 선형 색공간에서 동작 — 감마 기준 임계가 어긋나 CPU 이진화로 대체 (축: G−max(R,B), 임계 0.24)
3. 여백 없는 소형 텍스트("5/9")는 무시됨 → 28px 흰 패딩 필수
4. 점선 슬래시로 관측이 쪼개지면("9/"+"6") 고립 글리프를 문맥 없이 읽어 9→6 오독 → "관측 1개만 채택" + 팽창 재시도 사다리
5. 오독 문자 흡수: 슬래시→':' 등, 잡음 글리프 접두("j53") → 파싱에서 정제. 판독 속도는 16건 일괄 1.6초(프레임 로딩 포함, 건당 ≤100ms) — 2Hz 주기 예산 내
- t=900 프레임은 유닛 글로우가 둘째 자릿수를 가려 **사람도 정답 확정 불가**(인접 프레임 57→54 사이) → 픽스처에서 제외, t903으로 대체. 이런 단발 오독의 최종 방어선이 SupplyGate(§6.1)라는 설계 논리를 실물로 확인한 사례

**1단계 리뷰(3차원 병렬 + 반증 검증) 반영 결과**: 확정 버그 1건 수정(종횡비 검증을 비율 오차→픽셀 오차 판정으로 — 1750×1230 창이 통과해 하단 크롭 12px 어긋나는 경로 차단), FixtureSource 계약 결함 수정(stop() 무력화, 스킵 시 주입·타임스탬프 어긋남, 사전순 정렬), 테스트 공백 5건 보강(순서 검증 실질화, supply/↔expected.json 상호 완전성, mismatch→nil 리더 수준 검증, 오독 파싱 계약 고정, 스케일 해상도 종단 — 저해상도에서 슬래시 소실 실측 발견 → 관측 1개 게이트 하 2그룹 해석 + 적응형 확대 배율로 해결). 반박 기각 1건(CGContext 포인터 수명 — 형식 위반이나 재현 불가). **잔여 항목(후속 단계로 이월)**: ① supply 픽스처가 프로토스 단일 게임 16장 — §10 "종족 3종"·§9 "20장" 요건은 테란·저그 녹화 확보 시 충족 ② OCR 3단 사다리 최악 지연(~수백 ms)과 §4.6 "틱 합계 33ms" 예산의 정합은 3단계 Pipeline 설계 시 결정(후보: OCR 비동기 오프로딩 또는 supply 틱 예산 별도 계상).

**2단계 구현 노트 (2026-08-22 완료 — 실기 검증 대기)**:
- 페이즈 시그니처 실측 임계: 로비 = lobbySlots 영역 적갈색(r>90,g<55,b<55) 비율 ≥0.004 && 어두움 비율 ≥0.65 (로비 0.0095/0.797, 최악 비로비 0.0015). 인게임 = supply OCR 매치. 잠정 ended = 5초 소실 + 벽시계 보간 틱(정적 화면에서 SCK 무프레임 대비)
- **15분 실녹화 전체 회귀 통과**: 관측 1,805회, 전이 3건(로비 0.5s → 인게임 12.7s → 잠정 ended 920.1s), 오발 0건 — `SCCOACH_VIDEO_REGRESSION=1 swift test --filter PhaseVideoRegression`
- **픽스처·파일 소스의 함정 2개 실측 발견**: ① 녹화 추출 PNG는 BT.709 태그 — sRGB 컨텍스트로 로드하면 색 변환으로 임계 붕괴(적갈색 r120→90 미만). 로드는 이미지 자체 색공간으로 무변환 필수 ② push+bufferingNewest(1) 파일 소스는 소비 속도에 따라 프레임 유실(0.5s 샘플링→~2s 붕괴 실측) — §10 결정성 위반. ReplayFileSource는 pull 기반(unfolding)으로 구현
- 2단계 리뷰(3차원 19에이전트 + 반증): 확정 버그 3(LiveCapture 데이터 레이스, permissionLost 무한 스캔, MainActor 동기 OCR로 UI 정지) + 확정 리스크 6(start/stop 경합 고아 스트림, 리사이즈 실패 고착, 정적 화면 잠정 ended 미발동, userStopped 오분류, 인덱스 PNG 색 드리프트, replayBar 스텁 문서화) 전부 수정·반영. 반박 기각 2(재진입 시나리오 — 호출자 부재, 시계 재시작 — LiveCapture는 단조 PTS)
- 잔여: replay 판정은 스텁(리플레이 화면 픽스처 필요), 승패 중앙 밴드 양성 픽스처 없음(자연 종료 게임 녹화 필요)
- **실기 검증 완료(2026-08-22)**: 실기 SC:R 창 1937×1222(마우스 리사이즈 임의 크기)에서 자동 발견 → idle→lobby → lobby→inGame(+유도 좌표 실신호 확인·캐시 저장) → inGame→lobby 전이 로그 확인. 앵커 유도 모델·로비 시그니처가 두 번째 실물 크기에서 실증됨. clock 좌표의 타 크기 검증은 3단계 ClockReader에서

**3단계 구현 노트 (2026-08-22)** — 미결 결정 4건 확정(코드 주석에도 기록):
- **B-5** SupplyGate 첫 채택 = 연속 두 관측이 |Δ|≤2로 정합할 때 둘째 값 (시작 오독이 기점을 오염시키는 경로 차단, 지연 1주기)
- **B-9** GameClock "2회 연속 일치" = 상호 정합(두 관측의 게임시간 차 ≈ 스트림시간 차, ±1.5초)로 재정의. 예측 대비 ±5초 초과·감소는 보류
- **B-2** location=nil 알림(supply.block) = 이어콘·링 없이 음성만 pan 0
- **B-6** rate≤0이면 supply.block 침묵 — "막힘 지속 경고"는 실사용 판단 후 별도 조건. 성장률 창 30게임초(초기값)
- B-3 부분: 종족 미상(4단계 전) supply.block 문구는 종족 중립 **"인구 막힌다"**
- 시계 OCR 실측: 글자는 어두운 청회색(밝기 55~120) — 임계 55·저채도·**팽창 1회 필수**("00:07"→"20:00" 오독 실측). t080→1:07, t150→2:17 등 전 검산이 rate 1.0과 일치
- **15분 녹화 알림 회귀 통과**: supply.block 14건 전부 실제 막힘 직전 순간(9/9·17/17·57/58·111/115 등), 발화는 inGame에서만, 폐기 412건(쿨다운·재생 중) — `SCCOACH_VIDEO_REGRESSION=1 swift test --filter AlertVideoRegression`
- 성능 노트: 인게임 틱당 OCR 3회(페이즈 supply + 추출 supply + 시계) ≈ 200ms — 2Hz 예산 내지만 페이즈/추출의 supply 중복 판독은 최적화 후보
- **설계 이탈 기록**: ① `Outcome.played(interrupted: Bool)` — 설계 §4.5의 payload 없는 `played` 확장. urgent 인터럽트 발생 여부를 CoreOutput play/interrupt 분기로 전달하기 위함 ② `AlertRecord.refireKey` 필드 추가(§11의 'once 키' 명세 반영)

**3단계 리뷰(3차원 17에이전트 + 반증) 반영**: 확정 버그 6건 수정 — ① GameClock 일시정지 오탐(1초 해상도 시계의 0.5s 샘플링에서 정상 플레이 절반이 정지로 오인 → 정체 스팬 ≥1.6s 판정으로 변경, 톱니 역행 해소) ② rate가 정지 스팬에 오염돼 0.5 고착(재개 시 기준점 재설정) ③ 보류 오독 대비 감소 판정 오류(기준을 앵커로) ④ ended(확정) 후 supply HUD 잔존 시 1초 만에 inGame 복귀해 알림 재개(확정 후 inGame 후보 차단 — 새 게임은 lobby 경유) ⑤ VoiceBank 렌더 continuation 영구 대기 가능(10초 타임아웃 + ko-KR 부재 로깅) ⑥ 버퍼 부재 시 버스 영구 점유(즉시 완료 처리). 리스크 2건 수정(SupplyGate 하드 폐기의 연속성 파기, 출력 장치 변경 completion 유실 → 구성 변경 통지 처리). 반박 기각 3건(concatenate 포맷 혼합 — 단일 유터런스 내 불변, 승격 UIEvent 부재 — 설계 §4.6 그대로, 큐 승격 도달 불가 — 단일 규칙 현재만).

**4단계 구현 노트 (2026-08-22)**:
- **실측 이탈(설계 §6.4 수정 대상)**: 커스텀 로비에는 슬롯 색상 표시가 없다 — "슬롯별 색상 픽셀 샘플"은 이 로비 유형에서 불가. 색 식별은 5단계 인게임 관측(고정 팔레트/뷰포트 폴백)으로 이동. 로비에서 얻는 것: 슬롯 라벨·조종자·종족·컴퓨터 여부
- 실측 레이아웃: 슬롯 = 라벨 행("예꾸") + ~42px 아래 [조종자("다크호스"/"컴퓨터")][종족("프로토스")] 행. 계정명은 조종자 칸, UMS 슬롯 이름은 라벨 — isMe는 양쪽 매칭(정확 일치 우선 → 퍼지(편집거리 1) 유일 매칭, 복수 매칭이면 포기). OCR 오독 실측: "아현장"→"아현쟝"(편집거리 1로 흡수)
- B-3 확정: supply.block 문구 = 내 종족별 분화(서플 지어/파일런 지어/오버로드 뽑아), 미상·랜덤은 "인구 막힌다". 4문구 전부 사전 렌더
- 리뷰(1+2 에이전트) 확정 리스크 2건 수정: 복수 isMe 임의 선택(→유일 매칭 규율), idle→inGame 축소 모드에서 직전 판 slots 잔존(→§6.5대로 빈 채 진행). 스케일 정합·"지도 지정" 오선택 배제·문구×쿨다운 무간섭은 리뷰가 실측으로 확인

**5단계 진행 노트 (2026-08-23~)**:
- **동맹창 판독 경로 신설 (사용자 제안 채택 — 설계 §6.4 보강)**: 사용자가 게임 중 동맹창을 열면 읽는다(읽기만 — §0 정합). 실측: 각 행 = [순수 색 스와치][이름][동맹 체크][공유 시야 체크], 자기 행은 없음. 체크 = 흰 X(밝은 픽셀 ≈467 vs 미체크 0 — 완벽한 이진). 스와치는 미니맵 블렌딩 없는 원색(보라 128,53,143 / 파랑 51,90,201 / 빨강 250,6,24 실측). 컴퓨터는 종족 부족명("퓨리낙스 부족" 등), 사람은 계정명 → 로비 슬롯과 대조 가능. AllianceReader가 OCR 앵커(제목 "동맹"+열 헤더) 기준 상대 좌표로 파싱 — 창 크기 무관. 관측은 observedPlayers로 §6.4 휴리스틱보다 우선. 픽스처: `alliance/` 2종
- **Shift+Tab 고정 팔레트 정확 RGB 실측 완료 (0단계 6번·D-3 해소)** — 2차 녹화(The Hunters 6인전, 512s)에서 토글 5회를 미니맵 색 시계열로 검출해 측정: **적 = 빨강 ≈ (216±24, 0, 0)** / **나 = 초록 ≈ (48, 240, 0~24)** / 동맹 = 노랑 추정(토글 순간 동맹 유닛이 미니맵 시야에 없어 미확정 — 팀전 녹화로 확정). **D-3 실증: 고정-초록과 플레이어-초록이 사실상 동일값** — 사용자는 두 게임 모두 초록 플레이어였음. §6.2의 이중 팔레트 매칭 + myBase 타이브레이크가 필수임이 확인됨. 미니맵 앵커 모델이 3번째 실물 크기(1914×1274)에서도 정합(도트 시계열이 정상 판독됨)
- 2차 녹화 추가 수확: 멜레 로비 레이아웃(이름·종족 같은 행, 3열, "무작위" 표기 — LobbyReader 지원 추가), 6인전 반투명 동맹창(동맹 2 체크 — AllianceReader 통과), 팔레트 양 모드 픽스처 쌍(`minimap/`), 대저그 교전 장면(추가 flash 시퀀스 후보 t≈345-360)
- **FlashDetector v2 (설계 §6.2 2차 재작성 — 실측 스윕)**: 피격 경보의 실제 신호는 도트 점멸이 아니라 **넓은 자기색 밴드의 대면적 동시 토글**. 셀 토글 횟수(설계 v1)는 행군 대열과 분리 불가(스윕: 어떤 파라미터도 오탐 0 불가 — 대열의 도트 간격이 셀 스케일에서 점멸과 동일). 확정 판별자: ① 프레임당 연결 토글 클러스터 ≥ **20셀**(64×64 그리드 — 정탐 최대 38 vs 오탐 최대 16) ② 같은 사이트(반경 5셀) 1.2초 내 2회 반복. 실픽스처 검증: 피격 시퀀스 발화·행군 시퀀스 침묵
- **5단계 종단 회귀 통과(2차 녹화 512s)**: 동맹창 자동 판독(16.5s — 정답 일치) → minimap.enemy "10시/9시에 적" 7건(초반 적 정찰 접근) → supply.block 8건. 교전 구간 flash 무발화 = 뷰포트 억제 정상 동작(사용자가 그 전투를 보고 있었음). 유도 좌표(1914×1274)로 전 구간 판독
- 5단계 v1 결정: 팔레트 명시 감지(§6.2 이중 매칭 카운트) 대신 **기준색 통합 매칭**(고정 3색 실측값 + 동맹창 관측색) — 미지 색 무시(오발보다 축소). 동맹창 미사용 게임의 플레이어 색 적은 침묵(고정 팔레트 사용 시 검출됨). B-1 isInCombat 임계 12픽셀, B-10 존 반경: 본진 0.18·경고 0.35 (전부 튜닝 다이얼)
- **5단계 리뷰(2차원 13에이전트 + 반증) 반영**: 확정 버그 4 수정 — ① 규칙 기아: 쿨다운 문장 고정 반환으로 두 번째 존 영영 미보고 → 규칙이 alertLog(문장 추가)로 쿨다운 가시성을 얻어 건너뜀 ② 중반 진입 시 카메라 위치를 myBase로 오확정(적 본진이 '본진'화) → 진입 경로(lobby만) 게이트 ③ 흰색 유닛 도트가 뷰포트 박스를 부풀려 억제 존 오염 → 형태 검증(크기·종횡비·윤곽 밀도) ④ FlashDetector 같은 프레임 인접 클러스터 상호 재발 즉발 + 스테일 diff → 이전 프레임 재발 필수·0.6s 간극 가드. 리스크 수정: 트래커 1프레임 소실 유예, replay→inGame 리셋 누락, 프리렌더 중 무음(비프 대체), 적 색 미확보 상태 줄 안내. 알려진 한계(문서화): 흰색 플레이어 도트는 뷰포트 판정과 충돌해 v1 미지원, observedPlayers colorKey 위치 기반 불안정(nit), 키별 클러스터링 다중 패스 성능(nit)

**5단계 실사용 1차 피드백 반영 (2026-08-23 저녁)** — 보고: ① 내가 공격 갔을 때 교전 알림 없음 ② 동맹 교전 알림 없음 ③ 동맹창 열어도 무반응. 원인·수정:
- **근본 원인(①)**: 실전 게임의 내 초록 도트가 (0,240,48) — 기준 (48,240,12)와 거리 60으로 매칭 실패 → 내 색 마스크가 비어 깜빡임 검출 무력화. **게임 간 색 편차 실측**으로 기준색 체계를 기준별 임계로 개편: 나 (24,240,24)±48 · 적 (216,0,0)±44 · 동맹 노랑 (240,240,64)±44(실측 확정 — 고정 팔레트 스크린샷). 검산: 실전 화면에서 나/동맹/적 각 ~700/700/300px 매칭
- **②**: 깜빡임 마스크가 내 색 전용이었음 → 동맹 색 마스크 별도 검출 추가, 문장 "{존} 아군 피격" (카탈로그 43문장)
- **①-보조**: 원정 교전 밴드 실측 13~21셀 — 임계 20이 놓침 → 17로 하향(행군 오탐 최대 16 바로 위, 데이터가 허용하는 최저 안전선)
- **③**: 동맹창 프리게이트(남색 0.55)가 밝은 배경 위 반투명 패널을 미통과 → 0.35 완화 + 5초마다 게이트 무시 강제 판독(열어두면 늦어도 5초 내)
- 잔여 확인 필요: 뷰포트 억제 때문에 "보고 있는 전투"는 여전히 침묵(설계 의도) — 사용자 기대와 다르면 튜닝 논의

**5단계 실사용 2차 피드백 반영 (2026-08-23 밤)** — 보고: 중반 이후 내 멀티의 이벤트(밀집 활동)와 "녹색 네모칸"(미니맵 핑 박스로 추정)을 공격으로 오인. 원인: FlashDetector의 신호(자기색 대면적 토글)는 멀티의 일꾼 밀집·건물 완성·핑 박스 점멸과 물리적으로 동일 — 검출기 층에서는 분리 불가. 수정: **MinimapFlashRule에 적 근접 게이트** — 교전은 양측이 보여야 한다는 원리로, 플래시 지점 반경 0.12 내 적 픽셀이 없으면 억제(내 피격·아군 피격 공통). 문서화된 한계: 은폐·버로우 유닛만의 공격은 적 픽셀이 없어 억제됨(드랍 감지는 8단계 AirUnitRule이 별도 커버). 실기 재검증 대기.

**6단계 구현 노트 (2026-08-23) — 맵 프로필 + 정찰 소거**:
- **미리보기 실측(헌터스 로비 픽스처 1914×1274)**: 우측 패널에 "지도 이름: The Hunters"·"크기: 128x128" OCR 가능 — **tileSize를 픽셀 추정이 아닌 OCR로 획득**. 미리보기는 어두운 적갈색 테두리(r30~100·g≤40·b≤45·r우세)의 정사각형, 내부 1312-1585 × 156-430 = **274² (오차 0의 정방)**
- **테두리 사각형 검출(런 매칭)**: 행·열 밀도 방식은 배경 아트(행성 적갈색 대역, 561px 행 실측)에 오염돼 폐기. 확정 알고리즘: 갭≤4px 허용 최장 런(120~700px) → 위·아래 런이 x정렬(±8)·정방 간격(±len/8)이고 좌·우 열 연결(≥50%)이 실증되는 쌍 중 최고 점수. 검출 결과 (1310,156,275²) — 실측과 2px 내 일치
- **스폰 마커 실측 8색**(미니맵 팔레트 계열 — 클래식 원색보다 어두움): 빨강(224,8,8)·파랑(8,56,184)·청록(48,124,112)·보라(104,48,120)·주황(224,124,24)·갈색(96,28,8)·흰색(200,208,200)·노랑(240,240,56), 임계 ±40. 갈색↔흙 지형, 청록↔미네랄 프린지가 색 공간에서 겹침 → **컴팩트 블롭 검증**(6px↑·마커 치수 상한·fill≥0.3) + **시안 인접률 게이트 0.6**(프린지 = 시안 덩어리의 외곽선: 인접률 55~100% vs 진짜 마커 0% — 완벽 분리 실측). 청록 최대 블롭이 보라 마커 옆 프린지(17px)로 뽑혀 상호 간격 검증을 오염시키던 실패를 이 게이트가 해소
- 스폰 검증: 2~8개 + 전부 에지 대역(중심 거리 0.25↑) + **상호 간격 0.15↑**(UMS 트리거 마커가 한 줄로 몰리는 비표준 배열 배제 — Polypoid 로비로 음성 검증). walkable은 보수 분류(확실한 물·공허만 불가 — 헌터스 지형색이 저휘도라 어두움=불가 규칙은 오분류 실측, 우주 타일셋 임계는 8단계 실측 예정). MapStore 캐시(`~/Library/Application Support/SCCoach/maps/<slug>.json`) — 맵당 1회 분석
- **ScoutRule(§7)**: 초기화(spawns−myBase 최근접, 후보만 보관)/정찰 시작(본진 존 밖 이동 중 내 트랙)/소거(후보 반경 **0.08**(설계 0.06 → 미리보기 정규화 오차 흡수) 내 1.5게임초 체류 && 적 blip 0)/복구(소거 후보에 적 클러스터 → "{N}시 적 발견" 정정)/확정(잔존 1 → "{N}시 확정", oncePerKey 인덱스별 — 복구 후 재확정 자연 재발화). 개인전 전용(mode == .solo — allyObserved 파생). 카탈로그 +24문장(총 67)
- 리셋 계약 확장: any→lobby에서 `mapProfile = nil` (다음 판 맵 재획득), spawnCandidates·scoutStarted는 resetInGame 소관

**6단계 리뷰(3차원 17에이전트 + 반증) 반영** — 확정 12건 전건 수정, 반박 기각 2건(맵 이름 OCR 오채택 — 4스케일 실증으로 기각 / 비정방 정규화 부정합 — 정방 검증이 안전 무동작으로 차단):
- ScoutRule: ① 적 부재 판정을 현재 틱 blip → 체류 창 전체(적 트랙 이력 포함)로 — 경보 토글 저점·뷰포트 가림 1프레임 소실로 진짜 적 본진을 소거하던 경로 차단 ② 소거를 일시정지(§4.7 동결)·rate 비정상 대역(0.75~1.25 밖)에서 스킵 ③ 초기화는 최근접 스폰이 본진 반경(0.18) 내일 때만 — myBase 오확정 시 오정보 대신 침묵 ④ 복구를 onDelivery 원자화 — tip 드랍 시 "{N}시 적 발견" 정정이 영구 유실되던 경로 차단(드랍이면 다음 틱 재제안), 재복구는 문구 소진 상태로 상태만 복구 ⑤ 확정 발화 완료 후 재제안 금지 — dropped(alreadyFired) 레코드가 매 프레임 세션 로그를 오염하던 스팸 차단(everDelivered 헬퍼)
- 팀전 게이트 공백: 동맹창 미사용 팀전에서 scout가 solo로 오동작(동맹 본진을 적 후보화) → 동맹 색 blip 3프레임 누적(allySeenFrames)도 팀전 증거로 — 팀 매치는 공유 시야로 시작부터 동맹 blip이 보인다(실측)
- 캐시 오염 차단: MapStore.load에 불변식 검증(tileSize 16~256, walkable 길이, spawns 2~8·좌표 0~1 — 위반이면 재분석), 저장은 유효 프로필만(spawns=[] 캐시가 재분석을 영구 차단하던 경로 제거). 코어 내 디스크 IO(§10 위반)는 MapProfileStore 프로토콜 주입으로 — 기본 nil(IO 없음), 라이브 앱만 LiveMapStore
- 테두리 검출: 런 길이 한계를 패널 비례(하한 = 높이×0.10)로 — 절대 120px은 작은 창에서 구조적 실패. 선(얇음) 검증 추가 — 런 바깥 4px 행이 같은 스팬 40% 미만이어야(배경 아트 솔리드 적갈색 블록이 점수로 진짜 테두리를 이기는 경로 차단)
- **회귀 인프라 결함 발견·수정**: 비디오 회귀 3종의 녹화 선택이 movs.first/날짜 매칭 — 녹화 3개가 쌓이자 다른 해상도 녹화를 집어 0건 발화 오탐(실측: Alert 회귀가 헌터스/우주맵을 폴리포이드 좌표로 디코드). 해상도(naturalSize) 기반 선택으로 교체, Alert 회귀는 supply.block 필터(5단계 이후 미니맵 알림 공존). 구버전이 실캐시에 쓴 무효 폴리포이드 프로필(spawns 0) 제거
- 수정 안 함(문서화): 랠리 병력 1.5초 정지의 오소거 가능성(§7 체류 방식의 본질적 트레이드오프 — 반경 0.08이 기지 중심부라 초크 정지는 대부분 밖), 반경 이탈 1프레임의 dwell 리셋(보수 방향 — 소거 지연일 뿐), 마커 8색의 타일셋 일반화(정글 1건 실측 — 다른 타일셋 로비 픽스처 확보 시 재검, 오검출은 스폰 검증+캐시 게이트가 봉쇄)

**9단계 구현 노트 (2026-08-24) — 사후 리플레이 분석기 (§13)**:
- screp v1.13.3 소스 빌드(Go 1.27, arm64/amd64→lipo) → `tools/screp/screp` + LICENSE. 개발 탐색 경로: 번들 보조 실행 파일 → env SCCOACH_SCREP → `#filePath` 기준 tools/(개발)
- **screp JSON 스키마 실측** (픽스처 .rep 2개): ① **컴퓨터는 ID 255 + 커맨드 미기록** — 대컴퓨터전에서 상대 행동 대조 불가(분석 리포트에 명기) ② Build 커맨드 Pos=타일 좌표, Right Click·Targeted Order=픽셀(타일×32) ③ Fastest = 23.81 프레임/초 — 21511f=903s가 15분 녹화와 정합(시간축 검증) ④ 커스텀 맵 이름에 색 제어 문자(\x05 등)·U+FFFD 혼입 — cleanedMapName 정제 ⑤ WinnerTeam은 잔류 휴리스틱(0=미상) — 확정 아님을 리포트에 명기(§13)
- PostGameAnalyzer 3종 분석: 알림 타임라인 병기 / 팁-실행 지연(supply.block 발화→보급 건설 커맨드, OCR 앵커 오차로 발화 2초 전부터 인정) / **피격 오탐 의심**(사용자 요구 — minimap.flash 발화 존·±12초 창을 사람 상대 커맨드 위치와 대조: corroborated·suspectedFalse·indeterminate). 존 재현은 ZoneLabeler.label 재사용(문구 접미사 역파싱), myBase는 리플레이 StartLocation
- 점수 화면 보조 시그니처(§13 결정 확정): 잠정 ended에서만 상단 스트립(x 0~60%·y 0~9% — score_defeat_t918 실측 "패배!"+탭 행) OCR → 2틱 스트릭으로 확정 승격. inGame 중엔 검사하지 않아 오발 면적 최소. 메뉴 나가기 경로에서 gameEndedConfirmed(분석 트리거)가 살아난다
- 배선: CoachPipeline이 게임 경계별 알림 레코드 보관(§6.5 리셋 계약과 동일 경계 — ended 잠정 복귀는 유지) → AppCoordinator가 ended 확정 시 ReplayAnalysisFlow(detached, single-flight) → ReplayWatcher(AutoSave 신규 .rep, 크기 2폴 안정화) → screp → 분석 → `~/Library/Logs/SCCoach/<날짜>-<맵슬러그>.analysis.json/.md` + 상태 창 요약 줄(오탐 의심 건수 포함)
- 테스트 13종(실 .rep 서브프로세스 경유 — screp 부재 시 스킵): 파싱·정제·isMe·빌드 타임라인(첫 서플 62.6s 실측 대조)·팁 지연·오탐 판정·마크다운·JSONL 왕복·워처 신규 파일/타임아웃 + 점수 화면 승격(픽스처)

**9단계 리뷰(3차원 20에이전트 + 반증) 반영** — 확정 17건(중복 제거 10이슈) 전건 수정, 반박 기각 0:
- **워처 구조 결함(H)**: SC:R은 .rep을 종료 "직후" 쓰는데 ended(확정)은 5~7초 늦다 — 감시 시작 시점 베이스라인이 이번 판 .rep을 삼켜 메뉴 나가기 분석이 전멸 → 인정 기준을 "게임 시작 벽시계 이후 mtime"으로(코디네이터가 inGame 전이 시각 기록·전달, 미상이면 감시 시작 2분 전 폴백)
- **오탐 판정 오염 3종(H·M)**: ① isMe 미매칭 시 나를 '사람 적'으로 계산(내 커맨드로 corroborate) → 전건 판정 불가 처리 ② 팀전 동맹이 '적'에 포함 → 내 팀 제외 필터 ③ 창 내 상대 위치 커맨드 부재를 오탐 단정 → 자동 교전 가능성으로 판정 불가 강등. 존 대조도 정확 일치 → 허용 오차(시각존 ±1시간·본진 반경 +0.10·본진 근처 ±3)로 — 실시간 라벨(뷰포트 중심·미니맵 정규화)과 리플레이 재현(StartLocation·맵 픽셀 정규화)의 원천 차이 흡수
- **ended 오발 복구 불가(M)**: 중앙 밴드 OCR 2틱 오발이 즉시 확정 ended → inGame 복귀 영구 차단(코칭 사망) → 전이는 전부 잠정, 확정은 전이 후 스트릭 승격 전용(진짜 종료는 ~1초 지연으로 승격, 오발은 supply 재관측 복귀)
- **정적 화면 승격 불가(M)**: SCK 무프레임 + 승격 2연속 OCR 요구 충돌 → 잠정 ended·미확정에서 워치독이 마지막 프레임을 시각만 전진시켜 재주입(화면 불변이 무프레임의 전제)
- **행·경쟁·유실(M)**: screp 무타임아웃(30s 강제 종료 추가)·stderr 파이프 데드락 창(병렬 소진), 연속 두 판 확정 시 두 번째 분석 무통보 폐기(순차 체인으로), 레코드 스냅숏을 확정 방출 틱에 원자 수행(다음 게임 경계 리셋과 경쟁 차단), 팁-실행 지연 상한 60s·건설 1회 귀속·음수 지연 포맷, locateBinary 개발 경로 #if DEBUG 격리(릴리스 번들 누락 은폐 방지)

**9단계 실데이터 종단 검증 (2026-08-24)** — 사용자 실세션 로그(2026-08-23 23:34, Bottleneck 대컴퓨터전) × 그 판 AutoSave .rep로 오프라인 분석 성공: isMe·APM 116·팁-실행 지연 13건(오버로드 +6~46초, 무응답 7건) 산출. 이 과정에서 실결함 2개 발견·수정:
- **개인전 "아군 피격" 오발의 근원 확정**: 상대 컴퓨터 색 Olive(120,120,0)의 고휘도 토글이 동맹 노랑 기준(240,240,64)±44에 걸림 — 사용자가 보고한 "공격 안 당하는데 공격당한다" 오탐 15건의 정체. 수정: **개인전(mode==.solo)에는 동맹이 없으므로 아군 피격 알림 자체를 봉인**(MinimapFlashRule 팀전 게이트) — 색 임계 조정보다 원리적
- **isMe 국제화 실패**: 설정 "다크호스" vs 리플레이 계정명 "Darkhorse"(영문) — 편집거리 1 초과로 매칭 실패, 대컴퓨터전 전체에서 팁-실행 지연 누락. 수정: **사람이 1명뿐이면 이름 무관 나로 확정**(단일 사람 폴백)
- 부수 확인: 6단계 ScoutRule 실발화 확인 — 2인맵(Bottleneck)에서 게임 4초에 "9시 확정" 정상(후보 1개 즉시 확정은 2인맵의 자명한 동작)

**히스토리·복기 확장 (2026-08-24, 사용자 요청)**: ① HistoryIndex — 판마다 쌓이는 analysis.json 집계 → history.md(전적 표 + 추세: 팁 응답률·반응 중앙값·인구 알림/10분, 6판↑이면 최근 5판 vs 이전 비교). 판마다 분석 플로가 자동 재생성, 세션 로그는 30일 지나면 정리(분석 파일은 보존). 실데이터 베이스라인: Bottleneck 1판 — **팁 응답 46%(6/13)·반응 중앙값 27초·인구 알림 11건/10분** (11건/10분은 빈도 튜닝 후보 신호) ② 상대 빌드 복기 — opponentTimelines(사람·타 팀만, 컴퓨터는 커맨드 미기록), 마크다운 표시는 요약 뷰(일꾼 제외·유닛 종류별 첫 생산 = 조합 공개 시점, JSON엔 전체 보존) ③ 대인전 경로 테스트는 합성 ScrepOutput(JSON 디코드)으로 — 픽스처 .rep 2개가 전부 대컴퓨터전이라 corroborated/동맹 제외 경로는 합성으로만 검증됨(대인전 실리플레이 확보 시 실검증)

**주의력 지표 + 8단계 구현 노트 (2026-08-24)**:
- **macro.float "미네랄 뜬다"** (사용자 요구 "미네랄 + 동작 감지" — 7단계에서 주의력 성분만 발췌, 빌드 플랜은 효용 판단으로 보류): ResourceReader(미네랄 OCR — supply와 같은 초록 LED 폰트·실측 사다리 재사용 `readNumber`, ±2000 오독 게이트) + 카메라 이동 감지(뷰포트 중심 ≥0.01 이동 — 입력 후킹 없는 픽셀 프록시, §0 정합) → `attentionLapseScore()` = 미네랄 성분 0.6(절대량+상승폭) + 무동작 성분 0.4(5~12초). 임계 0.7, warn, 교전 침묵, 쿨다운 45s. 상수 전부 튜닝 다이얼(실사용 데이터로 조정 예정)
- **8단계 air.approach "공중 유닛 온다"**: Track.recentAirEvidence(최근 6점 중 걷기 불가 지형 수 — 다수결 ≥3로 지터 흡수) + 경고 존(0.35) 진입 시 urgent. **문구 중립화(사용자 확정)**: 도트만으론 셔틀/커세어/베슬 구분 불가 — "드랍 조심" 대신 직접 판별 가능한 사실만
- **3차 녹화 실측 — 종단 검증 계획 변경**: 녹화에 로비가 없고(치트 즉시 인게임), **미니맵은 지형 원천이 될 수 없음을 실측 확인** — ① 미탐사 안개 = 검정이라 공허와 미구분 ② 시야가 닿으면 공허도 성야 텍스처(sum 100~170)로 밝아져 플랫폼과 밝기 분리 불가 ③ 시야가 떠나면 다시 어두워짐(f409→f435 픽셀 시계열). 설계가 지형을 로비 미리보기에서 뽑는 선택의 실증. **8단계 종단 검증은 "로비 포함 실기 우주맵 1판"으로 이월** — 미리보기→프로필→공중 알림 전 경로가 그 한 판으로 검증된다. 수확: 드랍 런 실측(영상 409~414s, 셔틀 편대 회랑 횡단 ~0.03 정규화/초 — Tracker matchRadius 여유 4배 확인), 적 교전 플래시(418~424s)
- 신규 규칙 등록: rules [MinimapFlash, MinimapDanger, AirUnit, Scout, SupplyBlock, Attention], extractors에 ResourceReader(§4.6 Supply 다음). 카탈로그 +2문장(총 69)

**주의력+8단계 리뷰(3차원 16에이전트 + 반증) 반영** — 확정 13건(중복 제거 6이슈) 전건 수정, 반박 기각 0:
- **ResourceReader 게이트 재설계(H)**: 초판 ±2000 단순 게이트는 복구 경로가 없어 ① 첫 관측 오독이 앵커를 오염하면 판 전체 잠김 ② 정당한 대량 지출(2초에 2000+ — 알림에 반응해 한꺼번에 소비하는 타깃 시나리오!) 후 영구 동결 → SupplyGate의 실제 축소판으로: 첫 채택·±300 초과 점프는 **2회 연속 정합** 요구(4초 지연 비용), 평시 변화(±300)만 즉시. 단발 오독 즉시 채택 경로도 함께 소멸
- **idle 성분 신뢰 게이트 2개(H·M)**: ① 흰 도트·교전으로 뷰포트 검증이 죽으면 카메라 이동이 기록 안 돼 활동 중에 idle 만점 고착 → viewportValidAt(검증 성공 시각) 신선도 3초 게이트 — 검증이 죽은 구간엔 idle 성분 0(보수) ② 일시정지 스팬이 무동작으로 계산돼 재개 즉시 오발 → GameClock.lastResumeAtStream 기록, idle 기준점을 max(카메라, 재개)로
- **riseFrac 기준 상향(M)**: 순증 200/30초 만점은 평시 수입(중반 ~300)보다 낮아 상시 포화 → 250 시작·550 만점, 절대량 500~1000 — "수입이 안 쓰이고 쌓임"만 신호
- **공중 판정 재설계(H·M)**: 개수 다수결(6점=0.2초)은 상관 표본에 무력 — 정지 도트는 오분류도 정지라 경계 타일 양자화가 6/6으로 뚫림 → `isAirborne`: 시간 창 1.5초·스팬≥1초·표본≥5·**이동 변위≥0.03**(셔틀 실측 0.03/초)·불가 비율≥0.7. 정지 지상 유닛 오발 구조적 차단(정지 공중은 미탐 — 접근 경고 목적 부합)
- **resources rect 180px로 확장(M)**: 145px는 5자리 미네랄(10000+) 마지막 자릿수 잘림 — 부양 구간에서 리더 침묵. 가스 아이콘 전 여유 확인. 기존 사용자 좌표 캐시는 구폭 유지(삭제 시 갱신)
- **SSOT 갱신(M)**: 설계 §6.1(ResourceReader 2.0s·게이트)·§8 표(air.approach urgent "공중 유닛 온다"/macro.float warn "미네랄 뜬다" 확정값)·8단계 시그니처(isAirborne) — "결정값을 설계 문서에 기입" 규약 이행

**픽스처 적재 현황** (`SCCoach/Tests/Fixtures/`, 총 127 PNG ≈ 60MB):
- `supply/` 16장 + `expected.json` — 5/9~103/131, 미네랄·건물·유닛 겹침 난도 케이스 포함. 정답값 2회 독립 판독 교차 확인. `supply-scaled/` 1장(875×621) — §12.2 비율 스케일 경로 종단 검증용
- `phase/` 5장 + `expected.json` — 로비/인게임/나가기 대화상자/전환 일러스트/점수 화면
- `flash/` 정탐 53장 + 대조 53장(15fps 미니맵 시퀀스) + `meta.json`(시그니처 수치)
- `regions-1750x1242.json` — 이 픽스처 해상도의 영역 좌표(실측). supply·resources·clock·minimap 확정, lobbySlots·center 근사, replayBar 미확보
- 미확보 픽스처: 승리 화면, 리플레이 화면(replayBar), Shift+Tab 양 팔레트, 팀전, 빨강 적 오탐 케이스, 래더 로비 여부
